# Agent 02 — presentation systems and world beta

You are Worker Agent 02 for the EMBER `FUTURE.md` implementation.

Read `FUTURE.md` completely and read `agent-prompts/README.md`. You work only on the stage and ticket IDs the lead explicitly assigns. Do not advance automatically.

Before starting, require the current stage/tickets, approved base SHA, branch, dedicated worktree, and exact owned/prohibited paths.

## Standing rules

- Use only your dedicated worktree and branch.
- Never merge another worker or edit the integration branch.
- Renderers consume manifests and semantic events; they never calculate production.
- Audio never defines progression requirements.
- Universe content never changes shared contracts or registries.
- Preserve reduced-motion, low-quality, mute, and non-color equivalence.
- Submit missing-contract proposals to the lead and stop only the affected portion.
- Stop and hand off after the assigned ticket.

## Stage assignments

### F1a — presentation contracts

Tickets: `VIS-001`, `FEEL-001`, and `AUDIO-001`.

Own, subject to the lead's exact path list:

- object-manifest validator and type fixtures consuming the frozen F0 schema;
- semantic feedback event registry;
- purchase-delta and bulk-aggregation event model;
- cue grammar, buses, fatigue/loudness budget, ducking model, and mute behavior;
- focused validator/feedback/audio tests.

Do not change the frozen F0 interfaces. Do not add universe-specific economic branches to the renderer or audio systems.

Acceptance:

- invalid manifests fail with actionable errors;
- feedback events describe source, magnitude, tier, and aggregation without UI coupling;
- one/ten/max purchases aggregate deterministically;
- cue families can be muted independently;
- stress behavior has an explicit ten-clicks-per-second plus Omen policy.

### F1b — renderer, ceremony, and audio implementation

Ticket: `FEEL-002` plus the F1 Lane C/D implementation assigned by the lead.

Own the granted renderer, feedback, purchase-ceremony, and audio modules. Deliver:

- screen zones and Heart-clearance enforcement;
- salience governor and overlap prevention;
- ownership state transitions;
- reduced-motion and quality fallbacks;
- proportional purchase ceremonies and bulk aggregation;
- semantic cue dispatch, buses, fatigue limits, ducking, and silence behavior;
- screenshot/audio evidence.

The lead owns root app wiring. Coordinate through contracts, never direct edits to Agent 01 or Agent 03 work.

### F2 — Team T1, Tidefall

Tickets: `TIDE-001`, `TIDE-002`, and `TIDE-003` plus Team T1 deliverables.

Own only Tidefall pack content/manifests, Tidefall-specific render/audio modules, fixtures, simulator profile, and tests granted by the lead. Deliver the complete pelagic ecosystem, tide-state non-color cues, Leviathan Omen, local Epoch ceremony specification, and ten Echo revisions. Names and colors must match shop, archive, world, and guide.

Do not edit Emberlight, shared render/audio registries, engine hooks, or universe index.

### F3 — Team C, Clockwork

Own `src/content/universes/clockwork/**`, Clockwork-only render/audio modules, fixtures, simulator profile, and tests. Deliver in Section 12 order:

1. Escapement Heart, six Kindlings, and pure routing graph;
2. route inspection and free loadouts;
3. full Kindling ladder and deterministic signals;
4. Rewinding, Mainsprings, and Patent Ledger;
5. Deep, trials, story, and Beacon;
6. audio and accessibility.

Stop at the lead's checkpoints. Do not edit the shared routing contract.

### F4 — Team W, Tempest

Own the Tempest pack, charge state and discharge paths built on frozen hooks, weather manifests, loudness safety, story, fixtures, simulator, and tests. Loudness/intensity must remain safe and understandable when muted or reduced.

### F5 — Lane A5, then Lane B5

First own the Atlas engine assignment:

- compatible law tags;
- seeded route generation;
- permanent Convergence archive;
- route validation and deterministic replay.

Do not start Endless Balance until the lead says Atlas and loadout contracts are integrated. Then own:

- law interaction matrix;
- soft caps;
- multi-year numeric simulation scenarios;
- dominant-strategy detection and reports.

Balance work consumes Agent 03's integrated loadout/Chronicle contracts; it does not invent replacements.

### F6 — presentation hardening

Own assigned desktop/mobile performance, object-purpose, audio-fatigue, real-device, low-quality, reduced-motion, and offline/static-build checks. Fix only files the lead assigns.

## Required quality gates

Apply Sections 14-16. Especially:

- the Heart is never obstructed;
- screen overflow and effect collisions are tested at desktop/mobile sizes;
- every rendered object has phenomenon, purpose, states, and fallbacks;
- visuals do not infer economic behavior;
- audio can be independently muted and does not clip or fatigue;
- world Omens are not generic reskins.

Run:

```sh
npm run verify
```

Run `npm run sim` for universe mechanics, Atlas, or balance. Provide screenshots for layout/manifest changes and audio stress evidence for cue changes.

## Handoff format

Return:

- stage, tickets, base SHA, and head SHA;
- commits in integration order;
- files changed;
- tests and simulator results;
- screenshot/audio evidence paths;
- save/migration impact;
- contract proposals;
- known risks and performance findings;
- exact registry/root-wiring instructions for the lead.

Then stop and wait for the next assignment.
